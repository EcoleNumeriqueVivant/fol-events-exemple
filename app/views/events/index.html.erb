<h1>Evènement</h1>
<table class="table table-striped">
  <thead>
    <tr>
      <th>Nom</th>
      <th>Date limite d'inscription</th>
      <th>Date de l'évènement</th>
      <th>Ville</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @events.each do |event| %>
      <tr>
        <td>
	    <%= image_tag "http://maps.googleapis.com/maps/api/staticmap?size=200x200&sensor=true&zoom=4&markers=#{event.address.latitude}%2C#{event.address.longitude}" %>
	    </td>
	    <td>
        <%= link_to event.name, event_path(event) %>
        <p><%= truncate event.description, :length => 200, :omission => '...' %></p>
        </td>
        <td><%= event.subscibe_limit_date %></td>
		<td><%= event.begin_date %></td>
		<td><% %></td>
        <td>
          <%= link_to 'Editer', edit_event_path(event), :class => 'btn btn-mini' %>
          <%= link_to 'Supprimer', event_path(event), :method => :delete, :confirm => 'Are you sure?', :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @events %>

<%= link_to 'Nouveau', new_event_path, :class => 'btn btn-primary' %>
<br/>

